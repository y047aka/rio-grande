{
  "scripts": {
    "start": "run-pty % elm-watch hot % vite --open",
    "build": "elm-watch make --optimize && vite build"
  },
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.7",
    "elm-watch": "^1.1.2",
    "run-pty": "^4.0.3",
    "vite": "^4.4.4"
  }
}
